---

- name: Install MySQL
  apt:
    name={{ item }}
    state=present
    update_cache=true
  with_items:
    - mysql-server
    - python-mysqldb
    - libapache2-mod-auth-mysql
  notify: start mysql
  tags: install
