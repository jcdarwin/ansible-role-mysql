---
# defaults file for ansible-role-mysql

ansible_role_mysql:
  user: root
  password: changeme
  dbs:
  - whatever
  - whichever
  db_host: localhost
  db_user: whatever
  db_password: changeme
  encoding: utf8
  collation: utf8_unicode_ci

